import { ConcactStringIngenieurPipe } from './concact-string-ingenieur.pipe';

describe('ConcactStringIngenieurPipe', () => {
  it('create an instance', () => {
    const pipe = new ConcactStringIngenieurPipe();
    expect(pipe).toBeTruthy();
  });
});
